.sidebar
  .user__info
    .user__info__name
      %p #{current_user.name}   
    .user__info__icon
      = link_to new_group_path do
        %i.fa.fa-pencil-square-o
      = link_to edit_user_registration_path do
        %i.fa.fa-cog    
    .group
      -@groups.each do |group|
        = link_to group_messages_path(group.id), class: "sidebar__group__info" do
          %p.group__info-name  
            #{group.name}
          -if group.messages.last.present?
            %p.group__info-messages 
              = group.messages.last.body
          -else
            %p.group__info-messages まだメッセージはありません。